name = "WYT-ai-nb/moonabi"

version = "0.1.0"

readme = "README.mbt.md"

repository = "https://github.com/WYT-ai-nb/moonabi"

license = "Apache-2.0"

keywords = [ "moonbit", "ffi", "abi", "ci", "infrastructure" ]

preferred_target = "native"

description = "Cross-platform C ABI contract verification for MoonBit native bindings"

import {
  "moonbitlang/x@0.5.5",
}
